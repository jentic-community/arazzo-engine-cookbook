# Multi-Step Flow Recipe - Environment Variables
# This recipe uses the public JSONPlaceholder API which requires no authentication

# =============================================================================
# Arazzo Runner Configuration
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
ARAZZO_LOG_LEVEL=INFO

# Request timeout in seconds
ARAZZO_TIMEOUT=30

# =============================================================================
# Optional: For Testing with Mock APIs
# =============================================================================

# Set to 'true' to use mock API responses instead of real API calls
USE_MOCK_APIS=false

# =============================================================================
# Optional: Custom API Base URL
# =============================================================================

# Override the default JSONPlaceholder API URL if needed
# JSONPLACEHOLDER_BASE_URL=https://jsonplaceholder.typicode.com

# =============================================================================
# Notes
# =============================================================================

# This recipe demonstrates multi-step workflows with data passing between steps.
# It uses the JSONPlaceholder API which is free and requires no authentication.
#
# The workflow executes three sequential steps:
# 1. Fetch user information
# 2. Fetch posts by that user (uses output from step 1)
# 3. Fetch comments on the first post (uses output from step 2)
#
# No API keys or authentication are required for this recipe.